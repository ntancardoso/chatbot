{
  "name": "test-discord-bot",
  "version": "1.0.0",
  "description": "my first Discord bot!",
  "main": "dist/index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development node dist/index.js",
    "start:staging": "cross-env NODE_ENV=staging node dist/index.js",
    "start:prod": "cross-env NODE_ENV=production node dist/index.js",
    "build": "cross-env NODE_ENV=development rm -rf dist && tsc",
    "build:staging": "cross-env NODE_ENV=staging rm -rf dist && tsc",
    "build:prod": "cross-env NODE_ENV=production rm -rf dist && tsc"
  },
  "author": "You",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.3",
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "cross-env": "^7.0.3"
  }
}
